#!/bin/sh

ifinstalled tremc transmission-cli || exit

! pgrep -x transmission-da >/dev/null && transmission-daemon && notify-send "Starting torrent daemon..."

$TERMINAL --class Alacritty,tremc -e tremc
pkill -RTMIN+7 "${STATUSBAR:-dwmblocks}"
