#!/bin/bash

VIDEO=$(xclip -selection clipboard -o)

$TERMA --class pad -e bash -c "youtube-dl -c --add-metadata -i -o '~/dls/%(title)s.%(ext)s' --no-mtime $VIDEO --cookies $HOME/tmp/yt.txt;read -p 'da enter papai'"
