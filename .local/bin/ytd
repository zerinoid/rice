#!/bin/bash

VIDEO=$(xclip -selection clipboard -o)

$TERMA --class Alacritty,pad -e bash -c "yt-dlp -c --add-metadata -i -o '~/dls/%(title)s.%(ext)s' --no-mtime --extractor-args "generic:impersonate" $VIDEO;read -p 'da enter papai'"
